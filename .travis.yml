sudo: false

language: python
python:
  - "3.6.5"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest --cov=./
# test coverage stuff
before_install:
  - pip install codecov
after_success:
  - codecov --token=c6c00ecd-ca48-4248-91ef-761c77d5e559